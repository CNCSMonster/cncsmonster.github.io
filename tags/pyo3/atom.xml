<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh">
    <title>CCM 的博客 - PyO3</title>
    <link rel="self" type="application/atom+xml" href="https://cncsmonster.github.io/tags/pyo3/atom.xml"/>
    <link rel="alternate" type="text/html" href="https://cncsmonster.github.io"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>2026-08-31T00:00:00+08:00</updated>
    <id>https://cncsmonster.github.io/tags/pyo3/atom.xml</id>
    <entry xml:lang="zh">
        <title>PyO3 用法与踩坑</title>
        <published>2026-08-31T00:00:00+08:00</published>
        <updated>2026-08-31T00:00:00+08:00</updated>
        
        <author>
          <name>
            
              Unknown
            
          </name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://cncsmonster.github.io/posts/pyo3-usage-and-pitfalls/"/>
        <id>https://cncsmonster.github.io/posts/pyo3-usage-and-pitfalls/</id>
        
        <content type="html" xml:base="https://cncsmonster.github.io/posts/pyo3-usage-and-pitfalls/">&lt;p&gt;PyO3 用来连接 Rust 与 Python。常见做法不是只有一种：可以把 Rust 函数、结构体和模块暴露给 Python，也可以让 Rust 持有 Python 对象；还可以把 Python 创建的对象转换为 Rust 类型，继续使用已有的 Rust 业务逻辑。&lt;&#x2F;p&gt;
&lt;hr &#x2F;&gt;
&lt;h2 id=&quot;yi-ba-rust-han-shu-bao-lu-gei-python&quot;&gt;一、把 Rust 函数暴露给 Python&lt;&#x2F;h2&gt;
&lt;p&gt;最简单的用法是通过 &lt;code&gt;#[pyfunction]&lt;&#x2F;code&gt; 导出普通函数，再在 &lt;code&gt;#[pymodule]&lt;&#x2F;code&gt; 中注册。&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;use pyo3::prelude::*;

#[pyfunction]
fn add(left: i64, right: i64) -&amp;gt; i64 {
    left + right
}

#[pymodule]
fn calc(m: &amp;amp;Bound&amp;lt;&amp;#x27;_, PyModule&amp;gt;) -&amp;gt; PyResult&amp;lt;()&amp;gt; {
    m.add_function(wrap_pyfunction!(add, m)?)?;
    Ok(())
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Python 侧可以直接调用：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;python&quot; class=&quot;language-python &quot;&gt;&lt;code class=&quot;language-python&quot; data-lang=&quot;python&quot;&gt;import calc

assert calc.add(2, 3) == 5
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;这种方式适合计算函数、工具函数和性能热点。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;er-ba-rust-jie-gou-ti-bao-lu-wei-python-lei&quot;&gt;二、把 Rust 结构体暴露为 Python 类&lt;&#x2F;h2&gt;
&lt;p&gt;&lt;code&gt;#[pyclass]&lt;&#x2F;code&gt; 定义 Python 可见的类，&lt;code&gt;#[pymethods]&lt;&#x2F;code&gt; 暴露构造函数、方法和属性。&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;use pyo3::prelude::*;

#[pyclass]
struct Counter {
    #[pyo3(get)]
    value: i64,
}

#[pymethods]
impl Counter {
    #[new]
    fn new() -&amp;gt; Self {
        Self { value: 0 }
    }

    fn increment(&amp;amp;mut self) {
        self.value += 1;
    }
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Python 侧：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;python&quot; class=&quot;language-python &quot;&gt;&lt;code class=&quot;language-python&quot; data-lang=&quot;python&quot;&gt;counter = calc.Counter()
counter.increment()
assert counter.value == 1
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;这种方式适合需要在多次 Python 调用之间保留 Rust 状态的对象。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;san-jiang-python-shu-ju-zhuan-huan-wei-rust-lei-xing&quot;&gt;三、将 Python 数据转换为 Rust 类型&lt;&#x2F;h2&gt;
&lt;p&gt;PyO3 可以自动转换许多常见类型。例如，可以把 Python 字典提取为 Rust 的 &lt;code&gt;HashMap&amp;lt;String, f64&amp;gt;&lt;&#x2F;code&gt;：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;use std::collections::HashMap;
use pyo3::prelude::*;
use pyo3::types::PyDict;

#[pyfunction]
fn sum_values(values: &amp;amp;Bound&amp;lt;&amp;#x27;_, PyDict&amp;gt;) -&amp;gt; PyResult&amp;lt;f64&amp;gt; {
    let values: HashMap&amp;lt;String, f64&amp;gt; = values.extract()?;
    Ok(values.values().sum())
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;&lt;code&gt;extract()&lt;&#x2F;code&gt; 会创建 Rust 拥有的数据。如果只需要在当前调用中读取 Python 对象，可以直接使用 &lt;code&gt;Bound&amp;lt;&#x27;py, T&amp;gt;&lt;&#x2F;code&gt;，不必先把整个对象转换成 Rust 容器。&lt;&#x2F;p&gt;
&lt;p&gt;简单判断：&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;需要脱离 Python 生命周期保存或处理数据时，提取为 Rust 类型；&lt;&#x2F;li&gt;
&lt;li&gt;只在当前调用中读取对象时，优先借用 &lt;code&gt;Bound&amp;lt;&#x27;py, T&amp;gt;&lt;&#x2F;code&gt;。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;h2 id=&quot;si-zai-rust-zhong-bao-cun-python-dui-xiang&quot;&gt;四、在 Rust 中保存 Python 对象&lt;&#x2F;h2&gt;
&lt;p&gt;下面三个类型容易混淆：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;类型&lt;&#x2F;th&gt;&lt;th&gt;含义&lt;&#x2F;th&gt;&lt;th&gt;典型用途&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;Python&amp;lt;&#x27;py&amp;gt;&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;当前线程可以访问 Python 解释器的凭证&lt;&#x2F;td&gt;&lt;td&gt;调用 Python API、绑定 &lt;code&gt;Py&amp;lt;T&amp;gt;&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;Bound&amp;lt;&#x27;py, T&amp;gt;&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;绑定到当前 Python 上下文的对象引用&lt;&#x2F;td&gt;&lt;td&gt;当前调用中读取或调用 Python 对象&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;Py&amp;lt;T&amp;gt;&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;持有 Python 对象引用、但不绑定当前 &lt;code&gt;&#x27;py&lt;&#x2F;code&gt; 生命周期的智能指针&lt;&#x2F;td&gt;&lt;td&gt;在 Rust 结构体中保存 Python 对象，供以后使用&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;例如，Rust 结构体需要保存 Python 回调，等以后发生事件时再调用：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;#[pyclass]
struct Worker {
    callback: Py&amp;lt;PyAny&amp;gt;,
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;调用时再绑定到当前 Python 上下文：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;fn call(&amp;amp;self, py: Python&amp;lt;&amp;#x27;_&amp;gt;) -&amp;gt; PyResult&amp;lt;()&amp;gt; {
    self.callback.bind(py).call0()?;
    Ok(())
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;这里的 &lt;code&gt;Py&amp;lt;T&amp;gt;&lt;&#x2F;code&gt; 不是 Python 解释器本身，也不是 Rust 获得了 Python 对象数据的所有权，而是 Rust 持有一个由 Python 解释器管理的对象引用。&lt;&#x2F;p&gt;
&lt;p&gt;如果只需要当前调用中的临时访问，则使用：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;fn read_data(data: &amp;amp;Bound&amp;lt;&amp;#x27;_, PyDict&amp;gt;) -&amp;gt; usize {
    data.len()
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h2 id=&quot;wu-rang-python-ti-gong-rust-trait-suo-xu-de-ye-wu-luo-ji&quot;&gt;五、让 Python 提供 Rust Trait 所需的业务逻辑&lt;&#x2F;h2&gt;
&lt;p&gt;Rust 可以定义 Trait 和业务流程，但让 Python 对象提供具体实现。此时，Rust wrapper 保存 Python 对象的引用，并实现 Rust 所需的 Trait；Trait 方法被调用时，wrapper 再把调用转发给 Python 对象的方法。&lt;&#x2F;p&gt;
&lt;p&gt;下面的例子中，Rust 定义 &lt;code&gt;Model&lt;&#x2F;code&gt; Trait 和泛型函数 &lt;code&gt;solve()&lt;&#x2F;code&gt;，Python 定义 &lt;code&gt;UserModel&lt;&#x2F;code&gt; 并实现具体的 &lt;code&gt;compute()&lt;&#x2F;code&gt; 逻辑。&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;python&quot; class=&quot;language-python &quot;&gt;&lt;code class=&quot;language-python&quot; data-lang=&quot;python&quot;&gt;class UserModel:
    def compute(self, value):
        return value * 2

model = UserModel()
assert calc.solve(model, 10.0) == 20.0
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Python 调用 &lt;code&gt;calc.solve()&lt;&#x2F;code&gt; 时，传入的 &lt;code&gt;model&lt;&#x2F;code&gt; 是一个 &lt;code&gt;UserModel&lt;&#x2F;code&gt; 实例。Rust 对外暴露的函数接收这个对象，并将它保存到 wrapper 中：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;use pyo3::prelude::*;

trait Model {
    fn compute(&amp;amp;self, py: Python&amp;lt;&amp;#x27;_&amp;gt;, value: f64) -&amp;gt; PyResult&amp;lt;f64&amp;gt;;
}

struct UserModelWrapper {
    model: Py&amp;lt;PyAny&amp;gt;,
}

impl Model for UserModelWrapper {
    fn compute(&amp;amp;self, py: Python&amp;lt;&amp;#x27;_&amp;gt;, value: f64) -&amp;gt; PyResult&amp;lt;f64&amp;gt; {
        self.model
            .bind(py)
            .call_method1(&amp;quot;compute&amp;quot;, (value,))?
            .extract()
    }
}

fn solve_model&amp;lt;M: Model&amp;gt;(py: Python&amp;lt;&amp;#x27;_&amp;gt;, model: &amp;amp;M, value: f64) -&amp;gt; PyResult&amp;lt;f64&amp;gt; {
    model.compute(py, value)
}

#[pyfunction(name = &amp;quot;solve&amp;quot;)]
fn solve_wrapper(py: Python&amp;lt;&amp;#x27;_&amp;gt;, model: Py&amp;lt;PyAny&amp;gt;, value: f64) -&amp;gt; PyResult&amp;lt;f64&amp;gt; {
    let wrapper = UserModelWrapper { model };
    solve_model(py, &amp;amp;wrapper, value)
}

#[pymodule]
fn calc(m: &amp;amp;Bound&amp;lt;&amp;#x27;_, PyModule&amp;gt;) -&amp;gt; PyResult&amp;lt;()&amp;gt; {
    m.add_function(wrap_pyfunction!(solve_wrapper, m)?)?;
    Ok(())
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;完整调用链是：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;text&quot; class=&quot;language-text &quot;&gt;&lt;code class=&quot;language-text&quot; data-lang=&quot;text&quot;&gt;Python 创建 UserModel 实例
    → Python 调用 calc.solve(model, 10.0)
    → PyO3 将 model 接收到 solve_wrapper 的 Py&amp;lt;PyAny&amp;gt; 参数
    → Rust 创建 UserModelWrapper，并保存 Python 对象的引用
    → solve_model() 按 Model Trait 调用 wrapper.compute()
    → wrapper.bind(py) 获得当前调用中的 Bound&amp;lt;&amp;#x27;py, PyAny&amp;gt;
    → call_method1(&amp;quot;compute&amp;quot;, (10.0,)) 调用 Python 的 model.compute(10.0)
    → Python 返回 20.0
    → extract() 将返回值转换为 Rust 的 f64
    → solve_wrapper 返回 PyResult&amp;lt;f64&amp;gt;
    → Python 得到 20.0
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;这里没有把 &lt;code&gt;UserModel&lt;&#x2F;code&gt; 转换为等价的 Rust 结构体。&lt;code&gt;UserModelWrapper&lt;&#x2F;code&gt; 只是一个代理：Rust 通过它满足自己的 Trait 约束，但具体的 &lt;code&gt;compute()&lt;&#x2F;code&gt; 逻辑仍由 Python 执行。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;liu-jiang-python-dui-xiang-jie-gou-zhuan-huan-wei-rust-lei-xing-shi-yong-yi-you-de-rust-trait-shi-xian&quot;&gt;六、将 Python 对象结构转换为 Rust 类型，使用已有的 Rust Trait 实现&lt;&#x2F;h2&gt;
&lt;p&gt;Python 对象不能直接变成 Rust 的 &lt;code&gt;Box&amp;lt;dyn Evaluatable&amp;gt;&lt;&#x2F;code&gt;。当 Python 负责组合对象，而 Rust 已有一套 trait 业务逻辑时，可以使用三层结构：&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Rust 原生类型&lt;&#x2F;strong&gt;：实现 Trait，负责业务逻辑；&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;PyO3 包装类型&lt;&#x2F;strong&gt;：注册为 Python 类，负责让 Python 构造对象结构；&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;转换函数&lt;&#x2F;strong&gt;：遍历 Python 对象树，重建 Rust trait object 树。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;调用链如下：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;text&quot; class=&quot;language-text &quot;&gt;&lt;code class=&quot;language-text&quot; data-lang=&quot;text&quot;&gt;Python 构造 PyAdd、PyVar、PyConst 对象树
    → Python 调用表达式对象的 evaluate()
    → Rust 接收 Python 对象树
    → to_evaluatable() 按具体类型递归读取对象
    → PyAdd、PyVar、PyConst 转换为 AddExpr、VarExpr、ConstExpr
    → 形成 Rust 原生表达式树
    → 调用已有的 Evaluatable::evaluate()
    → 结果返回 Python
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;这里同时出现两个不同层次的转换操作：&lt;code&gt;vars.extract()&lt;&#x2F;code&gt; 是 PyO3 提供的对象方法；&lt;code&gt;to_evaluatable(py, obj)&lt;&#x2F;code&gt; 是本文定义的普通函数，负责按表达式树规则递归转换对象。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;1-rust-trait-yu-yuan-sheng-lei-xing&quot;&gt;1. Rust Trait 与原生类型&lt;&#x2F;h3&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;use std::collections::HashMap;

pub trait Evaluatable: Send + Sync {
    fn evaluate(&amp;amp;self, vars: &amp;amp;HashMap&amp;lt;String, f64&amp;gt;) -&amp;gt; Result&amp;lt;f64, String&amp;gt;;
}

pub struct ConstExpr {
    pub value: f64,
}

pub struct VarExpr {
    pub name: String,
}

pub struct AddExpr {
    pub left: Box&amp;lt;dyn Evaluatable&amp;gt;,
    pub right: Box&amp;lt;dyn Evaluatable&amp;gt;,
}

impl Evaluatable for ConstExpr {
    fn evaluate(&amp;amp;self, _: &amp;amp;HashMap&amp;lt;String, f64&amp;gt;) -&amp;gt; Result&amp;lt;f64, String&amp;gt; {
        Ok(self.value)
    }
}

impl Evaluatable for VarExpr {
    fn evaluate(&amp;amp;self, vars: &amp;amp;HashMap&amp;lt;String, f64&amp;gt;) -&amp;gt; Result&amp;lt;f64, String&amp;gt; {
        vars.get(&amp;amp;self.name)
            .copied()
            .ok_or_else(|| format!(&amp;quot;Undefined: {}&amp;quot;, self.name))
    }
}

impl Evaluatable for AddExpr {
    fn evaluate(&amp;amp;self, vars: &amp;amp;HashMap&amp;lt;String, f64&amp;gt;) -&amp;gt; Result&amp;lt;f64, String&amp;gt; {
        Ok(self.left.evaluate(vars)? + self.right.evaluate(vars)?)
    }
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;示例说明&lt;&#x2F;strong&gt;：这里的 &lt;code&gt;Send + Sync&lt;&#x2F;code&gt; 是 &lt;code&gt;Evaluatable&lt;&#x2F;code&gt; 这个业务 trait 自己声明的并发约束，不是 PyO3 的通用要求。只有当这些表达式对象需要在线程之间安全传递或共享时，才需要保留它。&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;h3 id=&quot;2-pyo3-bao-zhuang-lei-xing-yu-zhuan-huan-qiao&quot;&gt;2. PyO3 包装类型与转换桥&lt;&#x2F;h3&gt;
&lt;p&gt;包装类型注册为 Python 类，转换函数则按具体类型检查对象并递归构建 Rust 对象树：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;rust&quot; class=&quot;language-rust &quot;&gt;&lt;code class=&quot;language-rust&quot; data-lang=&quot;rust&quot;&gt;use pyo3::exceptions::{PyRuntimeError, PyTypeError};
use pyo3::prelude::*;
use pyo3::types::PyDict;

#[pyclass(name = &amp;quot;Const&amp;quot;)]
pub struct PyConst {
    #[pyo3(get)]
    pub value: f64,
}

#[pymethods]
impl PyConst {
    #[new]
    fn new(value: f64) -&amp;gt; Self {
        Self { value }
    }
}

#[pyclass(name = &amp;quot;Var&amp;quot;)]
pub struct PyVar {
    #[pyo3(get)]
    pub name: String,
}

#[pymethods]
impl PyVar {
    #[new]
    fn new(name: &amp;amp;str) -&amp;gt; Self {
        Self { name: name.into() }
    }
}

#[pyclass(name = &amp;quot;Add&amp;quot;)]
pub struct PyAdd {
    #[pyo3(get)]
    pub left: Py&amp;lt;PyAny&amp;gt;,
    #[pyo3(get)]
    pub right: Py&amp;lt;PyAny&amp;gt;,
}

#[pymethods]
impl PyAdd {
    #[new]
    fn new(left: Py&amp;lt;PyAny&amp;gt;, right: Py&amp;lt;PyAny&amp;gt;) -&amp;gt; Self {
        Self { left, right }
    }

    fn evaluate(&amp;amp;self, py: Python&amp;lt;&amp;#x27;_&amp;gt;, vars: &amp;amp;Bound&amp;lt;&amp;#x27;_, PyDict&amp;gt;) -&amp;gt; PyResult&amp;lt;f64&amp;gt; {
        let vars: HashMap&amp;lt;String, f64&amp;gt; = vars.extract()?;
        AddExpr {
            left: to_evaluatable(py, &amp;amp;self.left)?,
            right: to_evaluatable(py, &amp;amp;self.right)?,
        }
        .evaluate(&amp;amp;vars)
        .map_err(PyRuntimeError::new_err)
    }
}

fn to_evaluatable(py: Python&amp;lt;&amp;#x27;_&amp;gt;, obj: &amp;amp;Py&amp;lt;PyAny&amp;gt;) -&amp;gt; PyResult&amp;lt;Box&amp;lt;dyn Evaluatable&amp;gt;&amp;gt; {
    let obj = obj.bind(py);

    if let Ok(value) = obj.downcast::&amp;lt;PyConst&amp;gt;() {
        return Ok(Box::new(ConstExpr {
            value: value.borrow().value,
        }));
    }

    if let Ok(value) = obj.downcast::&amp;lt;PyVar&amp;gt;() {
        return Ok(Box::new(VarExpr {
            name: value.borrow().name.clone(),
        }));
    }

    if let Ok(value) = obj.downcast::&amp;lt;PyAdd&amp;gt;() {
        let value = value.borrow();
        return Ok(Box::new(AddExpr {
            left: to_evaluatable(py, &amp;amp;value.left)?,
            right: to_evaluatable(py, &amp;amp;value.right)?,
        }));
    }

    Err(PyTypeError::new_err(&amp;quot;Expected Const, Var, or Add&amp;quot;))
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Python 侧构造表达式并求值：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;python&quot; class=&quot;language-python &quot;&gt;&lt;code class=&quot;language-python&quot; data-lang=&quot;python&quot;&gt;import calc

x = calc.Var(&amp;quot;x&amp;quot;)
y = calc.Var(&amp;quot;y&amp;quot;)
expr = calc.Add(calc.Add(x, calc.Const(3.14)), y)

assert expr.evaluate({&amp;quot;x&amp;quot;: 10.0, &amp;quot;y&amp;quot;: 5.0}) == 18.14
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;这不是把 Rust trait 直接暴露给 Python，而是让 Python 包装类型充当接口，再转换成实现该 trait 的 Rust 对象。&lt;&#x2F;p&gt;
&lt;p&gt;PyO3 官方 Trait Bounds 教程解决的是另一类问题：让一个持有 Python 对象的 Rust wrapper 实现既有 Rust trait，再把这个 wrapper 传给带 trait bound 的泛型函数。它不讨论本文这种递归重建 &lt;code&gt;Box&amp;lt;dyn Trait&amp;gt;&lt;&#x2F;code&gt; 对象树的方案。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;qi-chang-jian-keng-dian&quot;&gt;七、常见坑点&lt;&#x2F;h2&gt;
&lt;h3 id=&quot;1-bu-yao-hun-yao-py-t-yu-bound-py-t&quot;&gt;1. 不要混淆 &lt;code&gt;Py&amp;lt;T&amp;gt;&lt;&#x2F;code&gt; 与 &lt;code&gt;Bound&amp;lt;&#x27;py, T&amp;gt;&lt;&#x2F;code&gt;&lt;&#x2F;h3&gt;
&lt;ul&gt;
&lt;li&gt;需要让 Rust 保存一个 Python 对象，供以后再次读取或调用时，使用 &lt;code&gt;Py&amp;lt;T&amp;gt;&lt;&#x2F;code&gt;；&lt;&#x2F;li&gt;
&lt;li&gt;已经处于 Python 上下文、只需当前访问时使用 &lt;code&gt;Bound&amp;lt;&#x27;py, T&amp;gt;&lt;&#x2F;code&gt;；&lt;&#x2F;li&gt;
&lt;li&gt;从 &lt;code&gt;Py&amp;lt;T&amp;gt;&lt;&#x2F;code&gt; 访问对象时，先通过 &lt;code&gt;bind(py)&lt;&#x2F;code&gt; 获得 &lt;code&gt;Bound&amp;lt;&#x27;py, T&amp;gt;&lt;&#x2F;code&gt;。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;混淆这两种类型时，常见后果是：试图把带有临时生命周期的 &lt;code&gt;Bound&amp;lt;&#x27;py, T&amp;gt;&lt;&#x2F;code&gt; 保存到长期存在的 Rust 结构体，或者忘记先调用 &lt;code&gt;bind(py)&lt;&#x2F;code&gt; 就直接访问 &lt;code&gt;Py&amp;lt;T&amp;gt;&lt;&#x2F;code&gt;。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;2-python-yi-chang-xu-yao-ying-she&quot;&gt;2. Python 异常需要映射&lt;&#x2F;h3&gt;
&lt;p&gt;Rust 业务层若返回 &lt;code&gt;Result&amp;lt;T, String&amp;gt;&lt;&#x2F;code&gt;，不能直接成为理想的 Python 异常。应在边界处把错误映射为 &lt;code&gt;PyValueError&lt;&#x2F;code&gt;、&lt;code&gt;PyTypeError&lt;&#x2F;code&gt;、&lt;code&gt;PyRuntimeError&lt;&#x2F;code&gt; 等具体异常。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;3-gen-ju-shi-ji-lei-xing-xuan-ze-zhuan-huan-luo-ji&quot;&gt;3. 根据实际类型选择转换逻辑&lt;&#x2F;h3&gt;
&lt;p&gt;当 Rust 收到的参数类型是 &lt;code&gt;Py&amp;lt;PyAny&amp;gt;&lt;&#x2F;code&gt; 时，需要在运行时判断它具体是哪一种 Python 对象。不要通过读取 &lt;code&gt;__class__.__name__&lt;&#x2F;code&gt; 等类名字符串来判断，因为普通 Python 类也可能使用相同的名字。&lt;&#x2F;p&gt;
&lt;p&gt;如果只接受已知的 &lt;code&gt;#[pyclass]&lt;&#x2F;code&gt; 类型，应使用 &lt;code&gt;downcast::&amp;lt;PyConst&amp;gt;()&lt;&#x2F;code&gt;、&lt;code&gt;downcast::&amp;lt;PyVar&amp;gt;()&lt;&#x2F;code&gt; 等方式检查对象的实际 PyO3 类型，并为每种类型执行对应的转换逻辑。所有检查都失败时，再返回 &lt;code&gt;PyTypeError&lt;&#x2F;code&gt;。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;4-bang-ding-dai-ma-yu-da-bao-shi-liang-jian-shi&quot;&gt;4. 绑定代码与打包是两件事&lt;&#x2F;h3&gt;
&lt;p&gt;PyO3 负责 Rust&#x2F;Python 绑定；要把扩展模块构建成 Python 可安装的软件包，通常还需要 &lt;code&gt;maturin&lt;&#x2F;code&gt; 等构建工具。模块名、Cargo 配置和 Python 包布局必须保持一致。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;can-kao-lian-jie&quot;&gt;参考链接&lt;&#x2F;h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a rel=&quot;noopener nofollow noreferrer&quot; target=&quot;_blank&quot; href=&quot;https:&#x2F;&#x2F;pyo3.rs&quot;&gt;PyO3 官方文档&lt;&#x2F;a&gt;&lt;&#x2F;li&gt;
&lt;li&gt;&lt;a rel=&quot;noopener nofollow noreferrer&quot; target=&quot;_blank&quot; href=&quot;https:&#x2F;&#x2F;pyo3.rs&#x2F;v0.23.5&#x2F;&quot;&gt;PyO3 用户指南&lt;&#x2F;a&gt;&lt;&#x2F;li&gt;
&lt;li&gt;&lt;a rel=&quot;noopener nofollow noreferrer&quot; target=&quot;_blank&quot; href=&quot;https:&#x2F;&#x2F;pyo3.rs&#x2F;main&#x2F;doc&#x2F;pyo3&#x2F;struct.Bound&quot;&gt;PyO3 Bound API&lt;&#x2F;a&gt;&lt;&#x2F;li&gt;
&lt;li&gt;&lt;a rel=&quot;noopener nofollow noreferrer&quot; target=&quot;_blank&quot; href=&quot;https:&#x2F;&#x2F;pyo3.rs&#x2F;v0.23.5&#x2F;trait-bounds&quot;&gt;PyO3 Trait Bounds 教程&lt;&#x2F;a&gt;&lt;&#x2F;li&gt;
&lt;li&gt;&lt;a rel=&quot;noopener nofollow noreferrer&quot; target=&quot;_blank&quot; href=&quot;https:&#x2F;&#x2F;docs.rs&#x2F;pyo3&#x2F;0.23.5&#x2F;pyo3&#x2F;&quot;&gt;PyO3 docs.rs&lt;&#x2F;a&gt;&lt;&#x2F;li&gt;
&lt;li&gt;&lt;a rel=&quot;noopener nofollow noreferrer&quot; target=&quot;_blank&quot; href=&quot;https:&#x2F;&#x2F;www.maturin.rs&#x2F;&quot;&gt;maturin 官方文档&lt;&#x2F;a&gt;&lt;&#x2F;li&gt;
&lt;li&gt;&lt;a rel=&quot;noopener nofollow noreferrer&quot; target=&quot;_blank&quot; href=&quot;https:&#x2F;&#x2F;doc.rust-lang.org&#x2F;reference&#x2F;types&#x2F;trait-object.html&quot;&gt;Rust Trait Object&lt;&#x2F;a&gt;&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
</content>
        
    </entry>
</feed>
